// Setting the border color for the vertical axis
lw_live_1.priceScale().applyOptions({
	borderColor: "#71649C",
	barSpacing: 20,
});

// Setting the border color for the horizontal axis
lw_live_1.timeScale().applyOptions({
	borderColor: "#71649C",
});

// Adjust the starting bar width (essentially the horizontal zoom)
lw_live_1.timeScale().applyOptions({
	barSpacing: 15,
});

// Customizing the Crosshair
lw_live_1.applyOptions({
	crosshair: {
	// Change mode from default 'magnet' to 'normal'.
	// Allows the crosshair to move freely without snapping to datapoints
	mode: LightweightCharts.CrosshairMode.Normal,

	// Vertical crosshair line (showing Date in Label)
	vertLine: {
		width: 8,
		color: "#C3BCDB44",
		style: LightweightCharts.LineStyle.Solid,
		labelBackgroundColor: "#9B7DFF",
	},

	// Horizontal crosshair line (showing Price in Label)
	horzLine: {
		color: "#9B7DFF",
		labelBackgroundColor: "#9B7DFF",
	},
	},
});

lw_live_1.applyOptions({
	layout: {
		background: { color: "#131722" },
		textColor: "#C3BCDB",
	},
	grid: {
		vertLines: { color: "#434651" },
		horzLines: { color: "#434651" },
	},
});

//-------------------------------------------------------

// Setting the border color for the vertical axis
lw_live_2.priceScale().applyOptions({
	borderColor: "#71649C",
	barSpacing: 20,
});

// Setting the border color for the horizontal axis
lw_live_2.timeScale().applyOptions({
	borderColor: "#71649C",
});

// Adjust the starting bar width (essentially the horizontal zoom)
lw_live_2.timeScale().applyOptions({
	barSpacing: 15,
});

// Customizing the Crosshair
lw_live_2.applyOptions({
	crosshair: {
	// Change mode from default 'magnet' to 'normal'.
	// Allows the crosshair to move freely without snapping to datapoints
	mode: LightweightCharts.CrosshairMode.Normal,

	// Vertical crosshair line (showing Date in Label)
	vertLine: {
		width: 8,
		color: "#C3BCDB44",
		style: LightweightCharts.LineStyle.Solid,
		labelBackgroundColor: "#9B7DFF",
	},

	// Horizontal crosshair line (showing Price in Label)
	horzLine: {
		color: "#9B7DFF",
		labelBackgroundColor: "#9B7DFF",
	},
	},
});

lw_live_2.applyOptions({
	layout: {
		background: { color: "#131722" },
		textColor: "#C3BCDB",
	},
	grid: {
		vertLines: { color: "#434651" },
		horzLines: { color: "#434651" },
	},
});

//-------------------------------------------------------

// Setting the border color for the vertical axis
lw_live_3.priceScale().applyOptions({
	borderColor: "#71649C",
	barSpacing: 20,
});

// Setting the border color for the horizontal axis
lw_live_3.timeScale().applyOptions({
	borderColor: "#71649C",
});

// Adjust the starting bar width (essentially the horizontal zoom)
lw_live_3.timeScale().applyOptions({
	barSpacing: 15,
});

// Customizing the Crosshair
lw_live_3.applyOptions({
	crosshair: {
	// Change mode from default 'magnet' to 'normal'.
	// Allows the crosshair to move freely without snapping to datapoints
	mode: LightweightCharts.CrosshairMode.Normal,

	// Vertical crosshair line (showing Date in Label)
	vertLine: {
		width: 8,
		color: "#C3BCDB44",
		style: LightweightCharts.LineStyle.Solid,
		labelBackgroundColor: "#9B7DFF",
	},

	// Horizontal crosshair line (showing Price in Label)
	horzLine: {
		color: "#9B7DFF",
		labelBackgroundColor: "#9B7DFF",
	},
	},
});

lw_live_3.applyOptions({
	layout: {
		background: { color: "#131722" },
		textColor: "#C3BCDB",
	},
	grid: {
		vertLines: { color: "#434651" },
		horzLines: { color: "#434651" },
	},
});
//-------------------------------------------------------

// Setting the border color for the vertical axis
lw_live_4.priceScale().applyOptions({
	borderColor: "#71649C",
	barSpacing: 20,
});

// Setting the border color for the horizontal axis
lw_live_4.timeScale().applyOptions({
	borderColor: "#71649C",
});

// Adjust the starting bar width (essentially the horizontal zoom)
lw_live_4.timeScale().applyOptions({
	barSpacing: 15,
});

// Customizing the Crosshair
lw_live_4.applyOptions({
	crosshair: {
	// Change mode from default 'magnet' to 'normal'.
	// Allows the crosshair to move freely without snapping to datapoints
	mode: LightweightCharts.CrosshairMode.Normal,

	// Vertical crosshair line (showing Date in Label)
	vertLine: {
		width: 8,
		color: "#C3BCDB44",
		style: LightweightCharts.LineStyle.Solid,
		labelBackgroundColor: "#9B7DFF",
	},

	// Horizontal crosshair line (showing Price in Label)
	horzLine: {
		color: "#9B7DFF",
		labelBackgroundColor: "#9B7DFF",
	},
	},
});

lw_live_4.applyOptions({
	layout: {
		background: { color: "#131722" },
		textColor: "#C3BCDB",
	},
	grid: {
		vertLines: { color: "#434651" },
		horzLines: { color: "#434651" },
	},
});